/**
 * Parser to turn string data into an AST
 */
var Parser = {
  // promise(source -> AST)
  parse(source) {
    // this can run in a service worker
    return new Promise();
  }
};

export default Parser;
